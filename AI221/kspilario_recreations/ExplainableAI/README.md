Notes:
* The data `titanic_train.csv` is copied here for reference.
* Should the accompanying notebook needs to be re-run, take note of the following:
  * If the notebook is to be re-run in Google Colab, upload `titanic_train.csv` in a convenient location within your Google Drive (example: put it in the same folder as the notebook), then mount the data.
  * If the notebook is to be re-run locally, just put `titanic_train.csv` in the same folder as the notebook then do necessary changes (with regards to the path of the file) in the notebook. 
